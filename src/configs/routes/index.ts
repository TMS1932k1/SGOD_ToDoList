import {
  RootStackRoutesType,
  RootNavigatorParams,
  RootStackNavigationScreenProps,
  RootStackRouteScreenProps,
  RootStackScreenProps,
} from './routeConfig';

export type {
  RootStackRoutesType,
  RootNavigatorParams,
  RootStackNavigationScreenProps,
  RootStackRouteScreenProps,
  RootStackScreenProps,
};
